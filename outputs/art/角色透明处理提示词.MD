# Background cleanup prompts

## Idle final attempt — original idle only

Result: SUCCESS — idle.png, 1254 × 1254. Sharp metadata: hasAlpha=true, channels=4. All four corners RGBA=[0,0,0,0]. Alpha counts: 777143 fully transparent, 1036 fully opaque, 794337 intermediate. Representative body samples have alpha=253 (~99.2% opaque), matching the existing satchel file behavior. Central belly RGBA=[253,216,154,253]. No visible background or checkerboard. Design, pose and framing visually preserved, but generative editing slightly repaints colored pixels and therefore does not guarantee byte-identical pixel preservation. Normal compositing can now be used instead of multiply for this idle sprite.

Use case: background-extraction
Input images: Image 1 is the sole edit target.
Primary request: make a clean PNG cutout of the existing toad by removing its white backdrop entirely. Export an actual transparent PNG with an RGBA alpha channel: empty space outside the toad must have alpha 0, and the colored toad body must remain opaque. There must be no background plane, no white or gray checker pattern, no painted transparency symbol, no ground and no shadow.
Preserve the existing original toad design exactly: same squat seated three-quarter pose, same face, amber sleepy eyes, bean-shaped glands, large olive spots, cream belly, dark brown contour, gouache texture, silhouette, size, placement and square canvas composition. No accessories and no new objects. Change only the backdrop into real empty transparency.

Mode: built-in imagegen only. No manual pixel editing or background keying.

## Attempt 1 result

Output: idle-alpha-attempt1.png (1254 × 1254)
REJECTED: Sharp metadata reports hasAlpha=false, channels=3. The checkerboard is painted into RGB. Corner pixels contain gray (rather than alpha 0). The toad was also slightly repainted with brighter eyes/lines, so exact character pixels are not preserved.
Reference satchel.png was independently verified as hasAlpha=true, channels=4, with all four corner RGBA samples [0,0,0,0]. Merely including it as a format reference did not make the idle output RGBA.
No dependent pose or prop cleanup calls were started, because the required idle proof did not succeed.

## Idle

Use case: background-extraction
Input images: Image 1 is the ONLY edit target, a sitting toad on white. Image 2 is ONLY a file-background reference: its empty outside area is real transparent alpha, and its painted subject is fully opaque. Do not include the bag from Image 2.
Primary request: Remove ONLY the white background from Image 1 and output the same toad as a transparent PNG with an actual RGBA alpha channel. Make all empty background pixels alpha=0. Keep every existing colored character pixel, silhouette, pose, composition, proportions, palette, dark outlines and internal gouache texture unchanged. Keep the entire toad interior fully opaque alpha=255 including its light cream belly and amber eyes.
Output requirements: TRUE PNG TRANSPARENCY with RGBA, like Image 2. No white backdrop, no gray backdrop, no black backdrop, no painted checkerboard pattern or grid. No new background. No shadow. No bag. No text.
The image will be overlaid on a wooden game floor with normal compositing: the floor must show only outside the toad, never through the toad body. Background extraction only, no restyling or repainting.
