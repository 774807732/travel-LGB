# 1.4.2 筒靴与 HTTPS 独立档验收

2026-09-14。应用 `3a636fea6375bf539587053200f7c25df1ff5b97`，唯一正式环境为 Mac mini。Chrome 正式 HTTPS 交互验收约于北京时间 18:19–18:35 完成；使用无痕窗口中的自建 `?debug=1` 标签，正常存档键 `travel-toad:v1` 未操作。所有导入/时钟推进仅作用于 `travel-toad:review:v1`。

## 替换与工程验证

- 两次内置 image_gen 编辑减少鞋面碎笔触；两次原图均为 RGB 假透明，随后按既有授权程序去背景、补留白，未冒称模型直接生成透明图。1254 方形 RGBA 定稿、300 方形 alpha WebP；浅/深底及 65/68/93px 检查通过。源图、实际提示词、处理记录见 `../art/source/rain-boots-v2/`。
- 显示为筒靴鞋，稳定 ID 仍 `gear_oilpaper_umbrella`，图片映射 `gear_rain_boots.webp`。价格 24 文、河街权重、三件用具总数、慢节奏及存档格式不变；保留旧伞原图/网页文件，当前界面不引用旧伞。
- 本地 `npm test` 55 项通过、构建通过；新增旧持有/行囊接续与在途/历史/奖励去重两项回归。Mini 稀疏源码不含原画归档，运行其余 51 项测试及构建通过，使用 Node 22.23.1。
- 45 项清单资源共 4,952,441 字节。Mini 新成品与本地 `dist/` 共 59 文件 SHA-256 全部一致；HTTPS 首页、新 JS/CSS、筒靴图均为 200，未关闭 TLS 验证。

## 正式 HTTPS 实际交互

| 检查 | 实际结果 |
|---|---|
| 新独立档与收成 | 24 文、0 趟开始；院坝收成后 36 文 |
| 小铺购买 | 筒靴 24 文后余 12，稀饭 8 文后余 4；用具为永久持有，稀饭 1 份 |
| 行囊与图像 | 选择稀饭及筒靴；纸面无棋盘底，黑胶本体和口沿完整，留白正常 |
| 自然首趟 | 未用快进：预备 → 自行出发 → 来信 → 归来；道明竹乡、竹影里打个盹、小竹垫 |
| 扣食/奖励 | 出发后稀饭 0；归来 4→12 文，完成 1 趟、见闻 1/12、纪念物 1/12 |
| 旧 ID 兼容 | UI 显示筒靴，导出文本的 ownedGear、在途 loadout 和 completed.loadout 均保留旧伞 ID |
| 刷新持久化 | 仍 12 文、1 趟、1 张、1 件，没有重复结算；减少动效开启状态保留 |
| 手账 | 12 个连续见闻位，1 已解锁与 11 未知；详情、返回、纪念物与旅程页通过 |
| 双页互斥 | 第二 HTTPS 页明确只读，操作禁用；主页面再买一份稀饭 12→4 后，第二页同步到 4 |
| 关闭后接续 | 关闭主测试标签，第二页恢复可写；能再次装稀饭和筒靴 |
| 取消预备 | 第二趟取消成功，home、4 文、稀饭仍 1 份、筒靴仍持有；首趟 15 秒窗口内未赶上取消，不算该项依据 |
| 导出下载 | HTTPS 设置中导出 JSON，Chrome 显示 1,430 B 下载完成；仅测试档保存于忽略的 work/test-results/https-boots-1.4.2/ |

## HTTP → HTTPS 迁移与恢复

1. 新开 `http://travel-lgb.duckdns.org/?debug=1` 为 24 文、0 趟，与现有 HTTPS 的 4 文、1 趟独立；HTTP 可写，无错误只读提示。
2. 通过设置 UI 将刚导出的 HTTPS 测试快照载入 HTTP（4 文、1 趟、1 份稀饭、筒靴），经过检查与确认两步；只用于构造已有旅行历史的迁移样本。
3. 在 HTTP 使用独立档「模拟离线一天」，收起 3 份收成后为 40 文；从「当前存档文本」读取导出 JSON。此模拟不是实际隔日回访，也没有修改系统时钟。
4. 切回 HTTPS，仍是 4 文、原 1 趟历史，未串档。输入非法 JSON 被明确拒绝，没有替换进度。
5. 将 HTTP 导出文本填写到 HTTPS 导入页，检查摘要为 40 文、1 趟、1 张，再确认替换。刷新后 40 文、1 份稀饭、筒靴、旅程/见闻/收藏/设置均保留。
6. 逐字段比较 HTTP 导出与 HTTPS 刷新后导出，除自然更新的 `lastSeenAt` 外差异为 0；没有重开奖或重发奖励。
7. 恢复上一份备份显示迁移前的 4 文、1 趟；确认恢复后回到 4 文且仍为 1 趟/1 张/1 件。最终 HTTPS 测试档留在该备份状态，HTTP 测试档为 40 文；均非玩家正常档。

## 网络、部署与边界

- 浏览器起初经系统代理连接正式 HTTPS 失败，而命令行直连 TLS 正常。用户明确授权后，仅向本机 Wi-Fi、AX88179A 代理绕过列表追加 `travel-lgb.duckdns.org`；原代理和所有原有规则原样保留，Chrome 随即可访问。
- 内置浏览器仍出现连接超时，未把它记为通过；未扩大直连范围、关闭证书验证、改 DNS 或读取 DuckDNS Token。
- Mini `current` 原子切换到 1.4.2 应用目录，旧 `7375ea8a7750e994689ab16b005262d30e34c96c` 保留回滚；本轮未重启服务。收尾文档仅同步源码，不重构建/重部署。
- Chrome 自建测试标签与本地 5173 预览均已关闭；正常存档、用户其他标签和既有代码未清理或覆盖。
- 未进行真机 Safari/Android、真实玩家隔日回访、压力测试或完整声音听感测试；本轮也未采集完整浏览器控制台错误清单，不将这些冒称通过。仅按本次需求完成筒靴和桌面 HTTPS 独立档流程，不扩展其他功能。
