# 1.4.5 四地第四卡 · 本地验收

2026-09-15，用户要求「将最后这版的素材和故事在本地配好」。本轮只完成固定流程的前两步，**未提交、推送或发布 Mini**。

后续标题微调（同日）：按用户原文把田坝第四卡改为「坝坝宴，安逸～」，插画和正文不动；63 项测试及构建再次通过，新产物 `game-BwVnc8Ks.js`。本地独立档刷新后，手账缩略卡与详情均显示新标题，仍 16/16、152 文；四文档与定稿说明同步，Mini 只读核对仍为原 1.4.4，未发布。

## 范围与来源

- 每地新增一张普通卡，本地 16 张见闻、4 个目的地、12 种收藏；不增加道具、食物条件、费用或奖励。原十二卡 ID、正文与图像不改。
- 抓天牛用 `cards-fourth-v2/card_daoming_04.png`，河街用 `cards-fourth-v1/card_river_04-v2.png`，采耳/寿宴用 `cards-fourth-v7/` 两图。后两张是增加约 30% 视觉纹理的树上定稿，不再重新生图或调纹理。
- 定稿逐字节复制到 `outputs/art/source/cards-fourth-v1/`，原候选、反馈裁图与提示词均保留；源图哈希断言见 `src/game/assets.test.ts`。
- 按现有编码脚本只处理四个新 ID：1536×1024 PNG → 1200×800 WebP、quality 88，完整 3:2。
- 新图字节：竹乡 243,508；茶园 270,304；河街 213,246；田坝 277,590。新增合计 1,004,648 字节；49 项清单资源共 5,957,089 字节，预算相应从 5 增到 6 MiB，每张新增图限制 300 KiB，不压低旧图质量。
- 采耳正文为树杈上看人类茶客；田坝标题改为「树杈上，看场寿宴」，正文同样向下旁观。低碎斑手绘、安逸节奏和玩法保持。

## 第一步：本地自动化与构建

- `npm test`：63/63 通过。增加四卡身份/普通卡/映射、人工旧十二卡档兼容、旧在途结果固定、新卡优先与来信/归来去重、定稿原图哈希/尺寸/体积检查。
- 100 组种子免费饭可收齐 15 张普通卡与 12 件收藏；稀饭条件卡仍独立，旧时间范围、存档锁、筒靴与布局回归保留。
- `npm run build`：TypeScript 与 Vite 通过。应用版本仅本地升为 1.4.5；产物 `game-VM9yIpPk.js`、`game-DIrvhMYZ.css`。
- 存档键仍 `travel-toad:v1` / `travel-toad:review:v1`，内部 version=2；未新增迁移或更改引擎/存储实现。

## 第二步：本地独立档试玩

使用构建预览 `http://127.0.0.1:5173/?debug=1`，内置浏览器。仅操作明确标注「独立测试存档」的页面，未打开正常游戏或读取正常存档。

1. 从设置导入 `scripts/create-card-review.ts` 生成的人工旧十二卡档；导入预览与手账均显示 12/16，四个新位置为不泄露图/标题的未知卡，旧十二卡可见。替换前按原程序保留测试档备份。
2. 固定测试种子 10，用页面的「用家常饭再出一趟」「推进到下一事件」驱动原有规则，而非直接授予奖励：第 13 趟抓天牛、第 14 趟送落叶、第 15 趟寿宴、第 16 趟采耳。每趟分别检查准备、出发、来信、归来状态，达到 16/16。
3. 抓天牛来信后刷新，仍为在途第 13 趟/13 张，随后正常归来；最后归来再刷新，仍 16 趟/16 张。页面导出文本显示四张新卡 count 均为 1，盘缠 120→152、收藏总件数 16，没有重复发奖。
4. 用设置展开的可复制文本再次导入，预览 16/16、16 趟、152 文，确认后仍可打开完整手账。
5. 实际查看四张卡的图像、标题、正文与获得次数：抓天牛、落叶图与故事一致；采耳是树上看别人采耳；寿宴是树上看七桌祝寿宴。详情返回手账正常。
6. 1366×900 桌面手账和寿宴详情；390×844 寿宴/采耳；360×800 采耳长标题/正文检查通过，图片比例与主体完整、正文可读，无横向溢出。保留三列连续卡墙，不加地点分组。
7. 浏览器捕获的警告/错误日志为空。视口覆盖已恢复；本地手账页作为交付预览保留，5173 仅监听回环地址。

工具过程中的初次预览未启动导致连接拒绝，启动后新标签正常；旧错误标签的 data: 页面未绕过策略读取。文件选择与一次文本读取曾遇工具延迟，最终通过游戏公开的导入/可复制文本界面完成，不改系统代理或安全设置。

## 正式环境与边界

- 开工只读核对：本地 HEAD / GitHub main / Mini source 均 `2456b9dbb2417718a9c6c1410898d5c14f6b7a3c`，Mini 干净。
- Mini `current` 仍为 `releases/e0177d42399662a1824c848cf27853e196705303`（1.4.4，十二卡），回滚目录未操作；没有修改线上文件、源码、进程、DNS、代理或 Token。
- 本地通过不代表正式 HTTPS 已验收。既有 1.4.3/1.4.4 HTTPS 交互补验事项仍保留；本轮不部署，不开展暂缓项目，不声称真机或真实隔日回访通过。
- 四份主文档已同步本地十六卡与 Mini 十二卡的区别。若后续授权发布，须使用当时实际构建重新核对，按固定四步流程发布 Mini 并验正式 HTTPS。
