# 设计规范

2026-09-15 1.4.3 节奏调整：后续出门后旅行随机 1–4 小时，准备仍为 2–5 分钟；首趟仍准备 15 秒＋旅行 90 秒，途中一半时捎信。已有在途旅程按原计划归来；离线只结算已备好的一趟，归来不自动再出门。帮助文案同步，不新增倒计时、催促或美术改动；本地验证通过，待 Mini/HTTPS 验收。

2026-09-15 域名答疑：域名只是访问名称，公网入口才是流量到达的地址/服务；二者不能混为购买一项即可解决 VPN。可评估获授权的现成备案域名子域名，未选定或实施；若最终改访问来源，仍需保留原档并导出/导入。

2026-09-15 网络体验边界：用户希望 VPN 开启时不需逐人设置网站例外。此目标由访问入口与客户端既有分流共同决定，不能用前端提示、JavaScript、HTTPS 或换域名后缀强制实现。大陆公网入口＋Mini 回源仅为待确认方案；无界面、美术、玩法或网络配置变更，不承诺任意 VPN/全局模式直连。

更新：2026-09-14｜Mini 1.4.2（`3a636fe`）已用筒靴鞋替换油纸伞：黑胶高筒、完整明暗、真透明与安全留白；原 65/68/93px 展示位不变。正式 HTTPS Chrome 的小尺寸显示、采购/行囊和存档交互已验收，场景、动效、见闻墙与安逸节奏不改。

HTTPS、HTTP 与旧 IP 的浏览器存档互不相通；切换前导出、切换后导入。HTTPS 仅保护连接，不表示游戏已向外网开放；界面不增加联网状态标识。

只读提示已分流：窗口占用说明可自动接续；存储不可用明确暂停操作、允许网站存储后刷新并可导出备份，不再误导回原窗口。保存成功后才提示采购/收成成功或收起行囊；失败不提前播放成功音效。无新增视觉资产。

最新线上布局：桌面三个簸箕在用户红圈位置左上至右下斜排，宽高各放大 20%；其余素材、收成规则与手机布局保持。

已上线尺寸：桌面簸箕在版本 8 基础上宽高各 ×1.2，原图不重采样，位置/角度/透明度和手机布局不变。

环境说明：正式环境为 Mac mini，当前 1.4.2；场景布局与动效沿用 1.3.1，见闻墙沿用连续排列。

## 风格与角色

- 十二件方案见 `outputs/art/candidates/souvenirs-twelve-v1/素材方案.MD`；新画琉璃熊猫、包谷、红苕、竹蜻蜓、小蒲扇、小木船、小南瓜，替换圆石后五件旧物复用。琉璃用少量整块高光/烟灰玻璃表现，竹木/叶脉/谷粒只留结构线；不靠碎斑填满。新图方形约 75% 占幅，先验证真 alpha，再沿用原收藏品尺寸；琉璃透亮不等于图片透明。
- 收藏品与见闻仅数量各为 3，不默认一一绑定。田坝原料是收藏品而非出发吃食；不新增烹饪/售卖/种植，也不把游戏原创礼物当作真实地方特产考据。
- 用户已将此前「只用模型处理」调整为「抠出透明 PNG 再用」。此次程序只去背景：彩色闭合轮廓保留内部高光/浅色材质，外部背景与草帽绳孔挖空；不全局删除白色、不把熊猫挖成空壳。
- 七件源图 1254×1254 RGBA，网页 300×300 PNG；大小沿用原展示位。浅/深底验收见 `outputs/art/source/souvenirs-alpha-v1/浅深底验收.png`。红苕原画碎笔触、高光等保留，不声称完成风格重绘。
- 筒靴鞋定稿：一双哑光黑胶农用高筒靴，宽椭圆口、棕赭/米黄口沿、收踝圆头、低平厚底；3/4 错落并置，不画腿/鞋带/高跟/品牌。内置模型两次编辑合并鞋面明暗，程序仅去假棋盘背景与补留白，不重绘鞋体；真实过程和提示词在 `outputs/art/source/rain-boots-v2/`。
- 筒靴 1254×1254 RGBA 源图 → 300×300 alpha WebP；主体最长边约占 80%，四边至少约 10% 留白。浅/深底及 65/68/93px 已检查，采用正常混合而非正片叠底，保留黑胶与口沿颜色。首版 RGB 候选仅作历史，不是正式资源。
- 田坝三卡已接入：四川平原稻田、田埂、水渠、林盘；见闻 1 用地头午饭修订图，2 吃撑摸肚子，3 草帽下小憩。粥碗/草帽去背景后用透明 WebP；去掉粥碗背景上的蒸气，保留碗、勺、红苕。不是新加可操作家景。
- 幺姑婆：普通直门襟扣衬衣、小翻领、挽袖、深色宽松长裤、劳动鞋、自然灰白短发或低束发；去掉交叠领、花布腰结、高髻与颈巾组合。以田边端碗拿筷、锄头放旁边的生活细节表现四川乡土，不使用地域面相或服饰脸谱。保留柔和手绘，地面、树叶与稻簇合并大色块，避免新增细碎斑点。
- 安逸、有牵挂、克制憨趣；柔和水粉/纸纹、深褐轮廓、灰绿与木色、靛蓝点缀。场景比角色更安静、更大。
- 新审美基线：第一轮约减半仍太花；第二轮必须以 `cards-clean-v1` 为底再减少约 60% 碎斑。优先树叶、草丛与大青砖：前景叶片主色 + 至多一个阴影面，草与远树合并成清楚的大簇，砖面保留完整底色和至多 2–3 块柔和色差；砖缝、结构线、角色主要斑点保留。不靠模糊、漂白或降对比代替重绘，不新增细碎纸纹。百分比是视觉目标，不作像素测量或累计比例承诺。
- 当前本地九卡为 `cards-clean-v2`：原生 1536×1024，网页 1200×800；保留各卡构图、天气与故事。叶片、草簇与青砖明显更完整；竹篱卡和桥洞卡的石阶立面仍有局部笔触。第二轮对照见 `outputs/art/review/九张见闻第二轮减纹理对照.html`，第一轮对照冻结保留。
- 四川平原林盘木屋、竹林水渠、竹编、坝坝茶、瓦檐河街；地方性通过行为与故事表达，不堆红金/辣椒/麻将。
- 宽胖低伏、头身无颈、琥珀半垂眼、豆形耳后腺、少量大疙瘩、奶油腹部；靛蓝花布包、小辣椒结。
- 角色画布约 96px，实际轮廓约 80–90px；在家不强制背包，打包/出门姿态带包。五姿态不改变游戏规则。
- 两景各有手机 3:4、桌面约 16:9 原创构图；横版按原生 1672:941 显示，不裁切拉伸。主角与包、信夹、簸箕、纪念物独立，不画进背景。十二卡均为 3:2。
- 旧六张 IP 板仅为概念参考；角色最终资产以 `public/art/characters/` 为准，不能继续裁整张分析板充当精灵。
- 生图素材全部落盘并记录实际提示词。透明必须检查 alpha；禁止把画出来的棋盘格当透明图。
- 角色五姿态与包/信夹/簸箕使用真透明、正常合成；吃食/用具通常在纸面适度正片叠底，红苕稀饭、草帽、筒靴使用正常混合。
- 出门信笺：暖象牙色手工纸、折痕卷角、牛皮纸信封、靛蓝花布签和竹叶；透明底 3:2。文字独立叠加楷体优先，保留“出去转转，莫急。—— 疙宝”。
- 十二种纪念物用真透明 PNG、正常合成；不加白底、边框、内距、旋转或矩形阴影。架上只留 `drop-shadow(0 2px 1px #4a382a30)` 轮廓投影。河街小画的纸张是物件本身，保留实色，透明仅限纸外背景。

## 交互与布局

- 主界面只放游戏：标题/盘缠 → 场景 → 信夹或院坝动作 → 状态 → 屋里/院坝/行囊/手账。
- 信夹在院坝有独立道具，屋里保留一条安静的快捷读信入口。
- Web 信桩按用户蓝框放在小铺左侧、后墙前沿，不放在小铺右边或院坝中间；木柱/底座属于背景，信封属于独立入口。移位必须同时修改背景、入口与碰撞，不能只挪信封。
- 当前 Mini 沿用历史版本 9 的院坝布局：小铺左侧靠墙信桩、留言统一 14px、信笺层级 5、簸箕左下斜排并放大 20%；不再以旧版中央柱/中央簸箕构图核对。Sites 保存版本仅作历史编号，不是当前发布目标。
- 行囊底部居中锚定：竖版左 50% / 上 72%；横版左 72.9% / 上 `calc(73.1% + 10px)`，10px 补偿图片下沿透明留白，让包底落在新桌面。宽 58px（手机 52px），点击与出门隐藏规则不变。
- 架上纪念物 54×54px、底部居中锚定：竖版左 14% / 上 70%；横版左 13.6% / 上 `calc(72.3% + 9px)`，落在小架上层。
- 横版信夹中心在 (70.2%,39.2%)，宽 56px，贴小铺左侧木柱正面；信桩碰撞 x=.62–.78、y=.31–.61，包含身体余量。
- 横版簸箕中心依次 (14.8%,77%)、(23.8%,84.2%)、(33.2%,91%)，按红圈斜向并列；当前单个宽 `clamp(69.12px,7.68%,92.16px)`（版本 8 为 57.6/6.4/76.8，各参数放大 20%），1:1 画布、统一旋转 -9°。坐标来自 `WIDE_HARVEST_TRAYS`，阻挡相对中心左右 .08、上 .07、下 .13，包含身体余量；原中央阻挡移除。提示条独立在 (50%,91%)，宽 180–240px；三簸箕及提示都可收成，容器与空隙不挡点地。
- 离家留言：竖版左 55% / 上 50% / 宽 60%（最多 264px），横版左 50% / 上 53% / 宽 264px；旋转 -4°。正文与落款统一 14px，由 `.away-note-copy` 设置、子元素继承；两端一致，不随窗口变字号，不拦截操作。
- 场景层级：簸箕 `z-index:3`、道具 `4`、出门信笺整体 `5`；信纸和正文一起盖过簸箕，保留 `pointer-events:none`，收成入口不失效。
- 场景热点与导航双入口；点击主角只说一句日常话，不给资源、不催行程。
- 点击地板触发完整蛙跳；远处按多次小跳抵达，路径绕开家具。无效落点只提示，不改变位置。
- 屋里阻挡：竹榻/陶罐、窗下柜、右柜、小格架、矮桌、竹篓/盆栽；院坝阻挡：门槛/石阶、竹货架、信桩、簸箕、两侧花木和竹篱。
- 横竖版×两景各记最后完成的落脚点；切景/切布局取消未完成小跳，缩放撤回到上次落地处，刷新回默认点。连点只保留最新目标，落地后再转向；旅行中不响应点地移动。
- 原生单层 dialog 内切换选择与详情；关闭/ESC/背景退出，焦点回入口；返回保留册页滚动位置。
- 手账三类：十二张见闻不分组，沿用固定三列网格连续排列；未知卡不泄露地点、插画与标题，已见卡可点开详情查看地点、次数和初次日期。纪念物仍按地点相邻排列，旅程记录与分母计算不变。
- 未读只用小朱红点；没有常驻秒级倒计时、强制领奖或稀有度。
- 声音、减少动效、备份与恢复在设置；替换存档必须先检查并再次确认。

## 样式参数

- 纸底 `#f3ebdd`，墨褐 `#4a382a`，苔绿 `#788151`，靛蓝 `#53677a`，朱红 `#a64a35`；分隔线 `#d9cfb9`。
- 系统中文无外部字体请求；主标题/卡标题宋体，正文系统黑体。见闻 16px、常用说明 14px、辅助信息至少 12px。
- ≥960px 为 Web 横版：场景最大约 1280×720（原生比 1672:941），按窗口宽高缩小，最小约 760px；壳增加 2px 边框，页面边距 24px / 16px。标题/盘缠同排，状态/导航同排。普通弹层 560px、大图弹层 720px，最大 88dvh。
- <960px 保留 390–440px 竖版壳，手机满可用宽；面板最大 420px、圆角 17px；原先已检查 360px，本轮不冒称新增真机测试。
- 两版角色均为 96px，行囊 58px（手机 52px）、架上纪念物 54px；不得对整个场景和角色一起做 CSS scale。
- 主要点击区域至少 44px；焦点框靛蓝 3px，背景热点有语义标签和悬停提示。
- 小卡略转 ±1°，纸边 5–6px，轻阴影；不做玻璃霓虹、厚重 3D 卡片。

## 动效与声音

- 点按 160ms、纸页 360ms、换景 240ms；缓动 `cubic-bezier(.22,.61,.36,1)`。
- 呼吸 4s、Y 缩放 1.012；姿态轮换 18s；出门移动/淡出 1s；信夹微摆 4s。
- 每跳 810ms：蓄力 140、蹬伸 80、后续腾空 340、前掌落地压缩 150、复位 100。只有腾空段推进位移，抛物线单峰；跳高 11–30px，每跳最多 74px，按当前场景宽高比算屏幕距离。
- `jump-atlas.webp` 为六姿态 3×2 图集；身体尺寸固定，实际画出后腿蹬直、空中收腿和前臂承重。透明裁片仅做对齐，不手工重绘。
- 朝向在起跳前调整；影子随高度缩至 62%、透明度 0.24→0.12，始终留在地面。减少动效时直接到达。
- 本地减少动效及系统偏好均关闭过渡与持续动画；状态仍清晰。
- Web Audio 合成低音量风声、纸页/布包声和收成三音；默认关，由用户操作开启，不复制原作音乐。

素材源 `outputs/art/source/`，网页资源 `public/art/`，提示词与最终 ID 映射 `outputs/art/`。通常只缩放/编码；此次用户另授权去背景，原图与处理记录独立留档。

设计稿、生成原图、透明成品与网页压缩资源均随 GitHub `main` 保存；运行时只引用 `public/art/`，避免误用候选图或白底源图。

Mac mini 正式版桌面/手机仍按 960px 断点使用同一套响应式规则。

九张见闻视觉核对：竹乡以竹林、竹编、叶儿粑为主；坝坝茶以盖碗、竹椅、树荫为主；河街以雨檐、临河石板路、桥洞框景为主。三组角色、布包与水粉质感一致。
