# 旅行癞疙宝

2026-09-15 标题微调：田坝第四卡现名「坝坝宴，安逸～」，正文与插画不变；仍仅在本地 1.4.5，未发布 Mini。

2026-09-15｜本地 1.4.5 已配好四地第四则故事，共 16 张见闻：抓天牛、树上看采耳、送落叶、树上看寿宴。采用用户确认的竹乡 v2、河街 v1 修订与茶园/寿宴 v7 纹理回补版，不再重绘。[定稿来源](outputs/art/source/cards-fourth-v1/素材说明.MD)、[本地验收](outputs/release/1.4.5-四地第四卡本地验收.MD)。63 项测试、构建、独立档四趟出游/刷新/图文/窄屏/导入往返通过。仅本地，未提交推送或发布，Mini 仍 1.4.4 / 12 卡；旧候选和提示词全保留。

2026-09-15｜1.4.4 已发布 Mini（`e0177d4`）：底部常驻「更换手机或浏览器，不会自动同步存档。」及备份迁移入口，设置中明确先导出再导入；不改存档或旅行。先通过本地 58 项测试、构建及独立档桌面/390/360px 显示、入口/返回/刷新/只读页面验收，再通过 Mini 54 项测试/构建后发布。HTTPS 资源/TLS 正常；正式内置浏览器连接失败，交互冒烟待补。旧版保留，无服务重启或正常档操作。

上一版 1.4.3（`d0287e3`）：后续正常旅行调整为出门后随机 1–4 小时，准备与首趟不变；已有在途档不重新计时。帮助文案同步，本地 57 / Mini 53 项测试及两端构建通过，HTTPS 资源/TLS 通过。该次独立档旅行交互验收缺口仍保留，不能用 1.4.4 提示验收替代。

2026-09-15 域名补充：公网入口不等于域名，购买域名不会自动获得大陆公网 IP。正式接入大陆网站/CDN 服务时需满足其域名与备案条件；有授权的现成备案域名可评估分配子域名，无需新买。参考：[阿里云大陆 CDN 域名要求](https://help.aliyun.com/zh/icp-filing/basic-icp-service/product-overview/use-alibaba-cloud-cdn)。未购买、改 DNS 或部署。

2026-09-15 网络答疑（仅方案）：当前域名指向公司私网，不能由网页保证任意 VPN 下自动直连。若希望利用用户已有的「国内直连」规则，可另评估中国大陆公网入口并加密回源 Mini；规则覆盖和优先级仍取决于客户端，全局代理不保证直连。若继续仅限公司使用，新增入口需另设服务端访问控制。未授权购买或新增公网入口，本次仅更新本地四文档，不改配置/部署。机制参考：[路由规则](https://wiki.metacubex.one/config/rules/)、[规则与全局模式](https://wiki.metacubex.one/config/general/#运行模式)。

上一轮：2026-09-14｜1.4.2 筒靴鞋替换已发布 Mini（应用 `3a636fe`），正式 HTTPS Chrome 独立档及 HTTP→HTTPS 迁移验收通过。55 项本地测试、Mini 51 项测试与两端构建通过；保留回滚、未重启服务或操作正常存档。接手先读 `HANDOF.MD` 顶部快照。

原九张见闻沿用第二轮减纹理图，随本次更新发布；[对照](outputs/art/review/九张见闻第二轮减纹理对照.html)、[提示词](outputs/art/见闻第二轮减纹理提示词.json)与旧图留档。

## 产品目标

[十二件收藏品](outputs/art/candidates/souvenirs-twelve-v1/素材方案.MD)：道明小竹垫/小竹篮/竹蜻蜓，坝坝茶小盖碗/小竹凳/小蒲扇，河街速写/琉璃熊猫摆件/小木船，田坝包谷/红苕/小南瓜。5 件复用、7 件抠图后接入；[透明验收](outputs/art/source/souvenirs-alpha-v1/浅深底验收.png)。收藏品不与吃食库存或永久用具混用。

已替换用具：[筒靴鞋正式素材](outputs/art/source/rain-boots-v2/素材说明.MD)。老式黑胶高筒农用雨靴，模型收整明暗后按授权程序去背景、补安全留白，使用真透明 WebP；替代油纸伞而非新增一格。稳定 ID 保持 `gear_oilpaper_umbrella`，旧持有者不重买，行囊/在途/历史原样接续；价格仍 24 文、河街倾向与接口不变。

田坝已作为第四个旅行目的地接入：见闻 1 用「地头」午饭修订图；见闻 2 仅在本趟带红苕稀饭时可得；见闻 3 为草帽下打盹。红苕稀饭描述「不要胀太多哦」，配套粥碗/草帽去背景后正常合成，不重画造型。

四川乡野里的轻陪伴旅行游戏。安逸、有牵挂、克制憨趣；原创癞疙宝、场景和见闻，借鉴放置旅行的节奏。

- 1 主角、屋里/院坝 2 家景，道明竹乡/成都坝坝茶/黄龙溪河街/田坝地头 4 地，本地 16 张见闻、12 种纪念物（Mini 仍为 12 卡）。
- 4 吃食、3 永久用具、1 货币盘缠；收成 → 采购 → 装包 → 自行出门 → 来信 → 归来。
- 免费家常饭无限保底，可得 15 张普通卡及全部收藏；条件卡仍只有红苕稀饭见闻。盘缠可由免费收成获得，无饥饿、死亡、战斗、签到、广告、付费、社交。
- 手账的 16 张见闻统一排列，每地补一张普通卡；未知卡只显示空白占位，解锁后才出现插画和标题，地点在详情中揭晓。纪念物与旅程页不变。
- 首趟确认后准备 15 秒，出门后旅行 90 秒，即确认后 60 秒信到、105 秒归来；后续准备随机 2–5 分钟、出门后旅行随机 1–4 小时，总计 1 小时 2 分钟至 4 小时 5 分钟。来信在旅程中点；已出发的旧旅程保留原时间。
- 初始 24 文与一份成熟收成；每 4 小时产一份，每份 12 文，最多留 3 份；归来 +8 文。
- 吃食 8 文，用具 24 文。确认只预留，出门扣吃食，取消不耗；相同行囊重复确认不延迟。
- 物品影响路线倾向，不指定目的地；连续同地两次后换地方；每地未见卡、未收纪念物分别优先。
- 时间戳驱动离线结算，只完成已备好的一趟，不自动续行。
- 在家时两景都显示癞疙宝；点击空地会跳过去，家具、门槛、货架、信桩与簸箕不能落脚。位置是当次页面的表现状态，不写入旅行存档。
- 蓄力 → 后腿蹬伸 → 空中收腿 → 伸前掌 → 落地压缩 → 复位；连点在落地后转向，远处按多次小跳抵达。
- 离家时显示独立手绘信笺；留言正文与“疙宝”署名统一 14px。信笺显示在收成簸箕前面且不拦截点击，不与收到的见闻卡混淆。
- 默认行囊摆在矮桌上；十二种归来纪念物使用 300×300 透明 PNG，架上没有白底纸签。
- 窗口宽 ≥960px 使用约 16:9 横版屋里/院坝，场景最大约 1280×720；新图原生 1672×941，不裁切拉伸。窄屏保留 3:4 竖版，角色保持 96px。横竖版各有家具碰撞和当页落脚点，不影响进度存档。
- Web 院坝信桩位于小铺左侧、紧靠后墙（以最新蓝框为准），信封入口和碰撞同步定位；原柱子所在空地可通行。手机竖版不变。

## 运行与交付

- 源码仓库：https://github.com/774807732/travel-LGB ，SSH 为 `git@github.com:774807732/travel-LGB.git`，主分支 `main`；代码、游戏素材、测试与交付文档保留完整 Git 历史。
- 正式环境（Mac mini）：https://travel-lgb.duckdns.org/ ，部署在 `/Users/jojo/Sites/travel-LGB`；独立 Caddy HTTPS 代理 `443 → 8788`，HTTP `80 → 8788` 与旧入口 http://10.131.75.39:8788/ 保留。仅可由能连接 Mini 内网的设备访问，未开外网或访问鉴权。
- Sites 已于 2026-09-12 永久删除；历史见 `outputs/release/发布记录.MD`。已移除 `.openai/hosting.json`，后续发布只更新 Mac mini，不重建 Sites。
- 本地开发入口：http://127.0.0.1:5173/ ，需先运行开发命令；不同网址存档独立。
- Mac mini 与本地预览的存档按网址隔离；可在设置中导出/导入。旧 Sites 存档不会自动迁入 Mac mini，已有导出文件仍可导入。
- Mini 连 `JOJO-Office` Wi-Fi（en1），游戏服务绑定所有网卡 `0.0.0.0:8788`，并未按 SSID 限制；能路由到 `10.131.75.39` 的其他内网/VPN 设备可访问。`10/8` 为私网地址，手机流量直接访问仍需另配公网入口。换正式域名后浏览器存档因网址变化仍需导出/导入。
- `brand-mini.local` 是 Bonjour 内网主机名，已在当前 MacBook 返回 200；其他设备是否可解析需逐台核验。公司 DHCP 提供的两台 DNS `10.130.205.51/.55` 对 `www.travellgb.com` 均返回 NXDOMAIN；若要这个未注册域名仅内网可用，须由公司 DNS 管理员添加内部记录。新主机名与 IP 属不同浏览器来源，原进度不会自动出现。
- 用户申请并设置 `travel-lgb.duckdns.org → 10.131.75.39`；HTTPS 证书、首页及主要脚本/样式/角色素材均验通。Caddy 通过 DuckDNS DNS 验证自动管理证书；Token 只保存在 Mini 用户目录，不进 Git。Mini 内网地址变化时仍需在 DuckDNS 更新 A 记录；外网版另需公网入口与安全方案。
- HTTPS、HTTP 与旧 IP 是不同浏览器存档来源：旧入口先在设置中导出进度，再到 HTTPS 入口导入。此前拟议的 `travellgb.com`、`travel-lgb.co` 均未启用。
- `npm ci` → `npm run dev`；固定访问 `http://127.0.0.1:5173/`。
- `npm run build` 输出 `dist/`；`npm test` 运行规则与存档测试；`npm run assets` 重新编码正式素材。
- `/?debug=1` 是独立测试存档，支持下一事件、离线一天和免费饭再出发；正常界面没有快进按钮。
- 第 1 步线框保留在 `outputs/wireframes/index.html`，不代表当前画面。
- 当前本地 63 项规则/存档/素材/碰撞/提示测试及构建通过；新增第四卡普通获得、旧十二卡与在途兼容、奖励去重、定稿源图哈希测试，保留时长与筒靴回归。49 项资源合计 5,957,089 字节；新增四张约 0.96 MiB，总预算从 5 调到 6 MiB，不降低旧图质量。旧伞仅为追溯/兼容保留。
- `node --import tsx scripts/create-card-review.ts` 可生成人工空档/旧十二卡档/十六卡档到忽略的 `work/test-results/fourth-cards/`。只在本地 `?debug=1` 设置中导入；种子 10 便于四趟覆盖新卡，不改正式游戏随机规则。
- 1.4.2 正式 HTTPS 独立档已验证收成、筒靴/稀饭采购装备、自然首趟、刷新、取消准备、手账、双页互斥/接续、导出下载、非法导入保护、HTTP→HTTPS 文本迁移及备份恢复；[历史记录](outputs/release/1.4.2-筒靴与HTTPS验收.MD)。1.4.3 浏览器交互仍待补验：用户已打开测试页，但工具仍定位到非游戏窗口，安全拦截后停止。真机 Safari/Android 与真实隔日回访仍单列待验。
- 使用系统代理时，内网游戏域名可能被送到无法连接 Mini 的代理出口；本机经用户授权仅给 Wi-Fi、AX88179A 添加该域名直连例外后 Chrome 恢复。其他设备按实际网络排查，不关闭全部代理、不清存档。
- 测试详情见 `outputs/release/制作与验收记录.MD`；素材和提示词入口见 `outputs/art/素材说明.MD`。

## API / 本地领域接口

没有后端 HTTP API，运行时不调用 AI。接口接收状态并返回新快照。

| 接口 | 职责 |
|---|---|
| `prepareTrip / cancelPreparation` | 配置、预留、取消行囊 |
| `advanceGame` | 按时间推进出发、来信、归来；固定结果并去重 |
| `routeWeights` | 吃食/用具倾向与连续重复保护 |
| `buyItem / harvest` | 采购、收成与盘缠 |
| `availableLetters / markLetterRead` | 来信列表与已读；读信不发奖 |
| `loadGame / saveGame` | 快照校验、迁移、自动备份及恢复 |
| `decodeSave / exportGame` | 导入检查、导出 JSON |
| `archiveBeforeReplace` | 主动替换前保留独立恢复备份 |
| `nextEventAt` | 测试时钟下一事件 |
| `sceneImage / getWalkMap` | 按横竖布局选择背景、碰撞和默认点 |
| `splitIntoHops` | 按宽高比与像素步幅拆分完整小跳 |

## 关键架构

- Vite + React + TypeScript；`App.tsx` 交互，`components/Sheet.tsx` 单层原生弹窗，`styles.css` 画面。
- `content.ts` 为稳定内容 ID，`engine.ts` 为纯规则，`useGame.ts` 管时钟/单写入者，`storage.ts` 管存档。
- `walkable.ts` 管横竖版资源、地板、家具及绕行；`hop.ts` 按实际宽高比和 74px 步幅分跳；`ToadActor.tsx` 同步精灵/位移/影子并处理缩放；`App.tsx` 按 `layout + scene` 保存当页最后落脚点。
- `home → packed → traveling → home`；归来未读独立，不阻塞下一趟。奖励与历史在同一快照提交。
- localStorage 键保持 `travel-toad:v1` / `travel-toad:review:v1`，内部版本仍为 2；旧档补红苕稀饭库存 0，圆石数量/在途/历史迁移到琉璃熊猫，不重发奖励。
- 单写入者优先 Web Locks；HTTP 缺少该接口时用 IndexedDB 排他事务 + 12 秒租约、2 秒续约。每次写入在事务中核验权限并重读最新 localStorage，休眠旧页不能覆盖接替者。
- 只读页不写坏档备份；关闭原页后约 2 秒接续，异常退出或后台暂停时租约到期后接续。页面恢复重新取锁；不依赖 HTTP 下缺失的 `crypto.randomUUID`。
- 区分另一窗口占用与存储不可用；后者暂停操作、保留已有存档、提示允许存储后刷新，不再误报原窗口。`act / replace / advanceDebug` 返回 Promise，界面等待成功后再提示或关闭面板。
- 1.4.1 本地 53 项测试/构建通过；真实 HTTP 独立测试档完成收成、采购、刷新保留、出发、双页互斥及接续；127.0.0.1 原生锁写入回归通过。
- Mac mini 当前应用 `e0177d4`（1.4.4），59 个成品文件 SHA-256 与本地构建一致；旧 `d0287e3` 发布目录保留回滚。新 JS/CSS HTTPS 200，TLS 校验通过；本轮无服务重启。详见 `outputs/release/发布记录.MD`。
- 本轮起点本地、GitHub 与 Mini 源码均为 `b221087`；保留并纳入本地此前四文档答疑与流程复盘改动，Mini 工作区干净。应用已同步并发布，后续文档提交仅同步源码、不重部署；无数据库/上传服务、环境变量或存档格式迁移需求。
- `:backup` 有效快照；`:damaged` 异常原文；`:before-replace` 主动替换前备份。坏档不静默清空，保存失败明确提示。
- 声音为本地 Web Audio 合成，默认关闭；图像由模型生成。此次用户授权程序去背景，`scripts/extract-souvenirs.mjs --tianba` 保留原图与处理记录；十二收藏用透明 PNG，其他资源 WebP，编码时保留 alpha。
- 个人进度是浏览器本地 JSON 存档，不是账号档，也不按 IP 识别人：同一设备、浏览器用户配置和来源（协议/域名/端口）共用一份正常档。操作/事件后保存，并定期落盘；重新打开按时间戳补算已备好的一趟。不同设备/浏览器/配置不自动同步；同一配置给两个人用会共用进度。无痕会话结束或清除网站数据可能丢档，本地自动备份也不能抵抗整站数据清除；请在设置导出 JSON 后手动导入迁移。2026-09-15 已核对代码，仅补充说明，无功能或部署改动。
- 正式构建为静态站点；游戏无数据库、账户系统或密钥需求。Mini 的 HTTPS 代理另需 DuckDNS Token 签发/续期证书，Token 不随构建发布。
- Mac mini 以 LaunchAgent 常驻服务提供构建产物，入口为 `current` 指向的发布目录；部署说明见 `deploy/mac-mini/README.md`。游戏进度仍在访问者浏览器 localStorage。

每次任务结束前同步本文件、`DESIGN.MD`、`MAPPING.MD`、`HANDOF.MD`。前期方案保留设计假设，实际进度以交接与验收记录为准。

固定发布流程（2026-09-15 用户确认）：**本地自动化与构建 → 本地独立档试玩通过 → 发布 Mini → 正式 HTTPS 检查。** 前两步未通过或受阻，不发布；正式 HTTPS 的资源/TLS 与相关独立档交互分别记录，未完成不宣称全部验收通过。1.4.3 遗漏本地浏览器交互便发布的缺口保留追溯，不能重复「先上线再补本地试玩」。仅文档变更不重新部署游戏。

素材核对：本地十六张见闻均为 3:2 横版 WebP，每地四张；新增四张仅编码定稿，不再次调纹理。程序抠图仅用于此前物品去背景，不声称红苕原画等残余碎笔触已重绘。
